import { createContext } from 'react'; // Correctly import createContext

// Create the context object
const UserContext = createContext();

// Export the context to use in other components
export default UserContext;